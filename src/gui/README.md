# GUI

This directory contains the GUI code for the project.

It is written in Rust, and to run it you need a Rust installed on your system to run it. You can install Rust using the following [instructions](https://doc.rust-lang.org/book/ch01-01-installation.html).

Once you have it installed, run `cargo run` in this directory.
